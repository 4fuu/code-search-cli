; Identifiers used as references
(identifier) @reference

; Type identifiers used as references
(type_identifier) @reference

; Property identifiers used as references
(property_identifier) @reference
